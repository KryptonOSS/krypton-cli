# Krypton-CLI
Krypton-cli is a CLI utility that uses Krypton library to perform cryptographic operations like encryption/decryption and creating secure vaults right from your terminal.

## Getting Started
    cargo build --release
    ./target/release/krypton help    # help arg will guide you
